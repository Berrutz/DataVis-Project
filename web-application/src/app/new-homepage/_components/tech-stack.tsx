import Container from '@/components/container';
import SectionTitle from './section-title';

export default function TechStack() {
  return (
    <section className="min-h-screen">
      <Container>
        <SectionTitle>Technological Stack</SectionTitle>
      </Container>
    </section>
  );
}

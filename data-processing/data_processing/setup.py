from setuptools import setup

setup(
    name="data_processing",
    version="1.0",
    description="",
    author="IncApache Group",
    author_email="",
    packages=["data_processing"],  # same as name
    install_requires=["pandas"],  # external packages as dependencies
)
